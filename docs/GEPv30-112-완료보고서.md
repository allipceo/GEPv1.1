# GEPv30-112 완료보고서 — MiniMock 문제 출처 표시

**작성일**: 2026-08-15
**작성자**: 고팀장 (Claude Code)
**작업 목적**: MiniMockQuiz 문제 칩 아래 원본 출처(회차·과목·과목 내 번호) 표시 추가
**브랜치**: `gepv30-112-source-display`

## 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `scripts/generateMiniMockSets.cjs` | `roundNumber` 필드 복사 1줄 추가 (`picked.forEach(...)` 블록) |
| `public/data/mini_mock_sets.json` | 30세트 재생성 (roundNumber 포함) |
| `src/pages/MiniMockQuiz.jsx` | 문제 메타 칩 아래 출처 텍스트 렌더링 추가 (272행 아래) |

## 테스트 결과

- 빌드: ✅ `npm run build` 성공 (오류 0)
- 세트 재생성 검증: ✅ 30세트 전체 `roundNumber` 1~40 범위, 결측 0건 (`node -e` 스크립트로 전수 확인)
- 브라우저 실사용 검증 (테스트 계정):
  - SET 05 Q1: `(원본: 25회 법령 6번 문제)` 정상 표시
  - "다음" 클릭 → Q2: `(원본: 24회 법령 7번 문제)` — 회차·번호 모두 정상 갱신
  - SET 10 Q1: `(원본: 31회 법령 4번 문제)` — 세트가 다르면 다른 회차 확인
  - `roundNumber == null` 방어 분기 정상 (모든 문제에 값이 있어 분기 발동 없음)
- 콘솔: 401 에러 1건 발견 — 오래된 dev 세션의 Supabase 토큰 만료로 인한 것으로, 정적 JSON 로드(`mini_mock_sets.json`, `exams.json`)는 인증이 필요 없어 본 기능과는 무관함을 확인

## 특이사항

없음. GEPv30-107~111 기준선(채점·통계·라우팅) 변경 없음.
